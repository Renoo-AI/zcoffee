rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /menu/{document=**} {
      // Tout le monde peut voir le menu
      allow read: if true;
      
      // Seuls les administrateurs autorisés peuvent modifier
      allow write: if request.auth != null && 
        (request.auth.token.email == 'belhajyoussefbelhaj@gmail.com' || 
         request.auth.token.email in ['awatifnefzi@gmail.com', 'mina.zina.coffee@gmail.com', 'admin.zina@gmail.com']);
    }
  }
}
